apiVersion: apps/v1
kind: Deployment
metadata:
  name: cc-groups-deploy
  labels:
    name: cc-groups-deploy
    app: chitychat
spec:
  replicas: 1
  selector:
    matchLabels:
      name: cc-groups-pod
      app: chitychat

  template:
    metadata:
      name: cc-groups-pod
      labels:
        name: cc-groups-pod
        app: chitychat
    spec:
      containers:
        - image: 192.168.69.1:5000/chitychat/cc_groups:2.0-dev
          imagePullPolicy: Always
          name: cc-groups
          command: 
            - cc_groups
          envFrom:
            - secretRef:
                name: chitychat-secrets

